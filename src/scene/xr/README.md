# src/scene/xr/

Sessão WebXR: entrada/saída de sessão, controllers, hands e locomoção.
Aqui vive a lógica que só roda quando `useXRSupport()` acusa suporte a
`immersive-vr`. Nada importa de `src/sections/`.

Preenchido na Task 2 (Portal 3D + sessão XR).
